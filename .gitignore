.DS_Store
*.swp
*.swo

# AI workflow scratch (ephemeral, per ~/.claude/CLAUDE.md convention)
.llm/

# Node / pnpm
node_modules/
pnpm-debug.log*

# Tauri / Vite build artifacts
# (Cargo.lock is intentionally NOT ignored — it's committed for applications
# to guarantee reproducible builds.)
app/dist/
app/src-tauri/target/
app/src-tauri/gen/schemas/

# Editor / OS
.vscode/
.idea/
Thumbs.db
